import { DerivativeDirective } from './derivative.directive';

describe('DerivativeDirective', () => {
  it('should create an instance', () => {
    const directive = new DerivativeDirective();
    expect(directive).toBeTruthy();
  });
});
